import numpy as np

# 1.2

arr_zeros = np.zeros(10, dtype=int)
arr_ones = np.ones(10, dtype=int)
arr_fives = np.full(10, 5, dtype=int)

print("Массив из нулей:", arr_zeros)
print("Массив из единиц:", arr_ones)
print("Массив из пятерок:", arr_fives)